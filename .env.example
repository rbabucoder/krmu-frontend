# Strapi CMS base URL for media assets.
# Leave empty when using Strapi Cloud (returns absolute URLs).
# Set to your Strapi host (e.g. http://localhost:1337) when self-hosting.
NEXT_PUBLIC_STRAPI_URL=
